const assert = require('assert');
const chessmultiplayer = require('../index.js');

describe('chessmultiplayer', () => {
  it('has a test', () => {
    assert(false, 'chessmultiplayer should have a test');
  });
});
